# SureWaka Infrastructure
# Start with managed services, migrate to custom infra as needed

# Placeholder - add provider config when ready to deploy
# Recommended: Start with Vercel (web apps) + Railway (API/workers)
# Move to AWS/GCP when you need more control

# terraform {
#   required_providers {
#     aws = { source = "hashicorp/aws", version = "~> 5.0" }
#   }
# }
